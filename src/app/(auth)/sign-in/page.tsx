"use client";
import { FC } from "react";
import SingIn from "@/components/SingIn";

interface pageProps {}

const page: FC<pageProps> = ({}) => {
  return (
    <div>
      <SingIn />
    </div>
  );
};

export default page;
